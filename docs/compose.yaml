services:
  docs-preview:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}
    ports:
      - "3000:3000"
    volumes:
      - .:/app/docs
