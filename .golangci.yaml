version: "2"

formatters:
  enable:
    - gofmt
    - gofumpt

linters:
  enable:
    - gosec
  settings:
    gosec:
      excludes:
        - G304
      severity: medium
      confidence: medium

issues:
  exclude-rules:
    - path: (.+)_test.go
      linters:
        - gosec
